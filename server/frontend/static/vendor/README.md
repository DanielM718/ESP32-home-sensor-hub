# Frontend Vendor Assets

`chart.umd.min.js` is installed on the Raspberry Pi by:

```bash
/opt/home-sensor/server/scripts/install_frontend_assets.sh
```

The file is intentionally not committed here because it is a generated third
party browser bundle.
