# This file is an example only. Copy it to secrets.yaml only if another local
# YAML customization needs these values. Configure the MQTT broker integration
# through Settings > Devices & services > MQTT; current Home Assistant versions
# store those credentials in /config/.storage rather than configuration.yaml.
mqtt_username: change_me
mqtt_password: change_me
