[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "butters-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["websockets==15.0.1", "cryptography>=46,<51"]

[tool.setuptools.packages.find]
where = ["src"]
