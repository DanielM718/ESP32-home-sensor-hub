#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
butters_dir=$(dirname -- "$script_dir")
export PYTHONPATH="$butters_dir/src${PYTHONPATH:+:$PYTHONPATH}"
exec "$butters_dir/.venv/bin/python" -m butters.web
