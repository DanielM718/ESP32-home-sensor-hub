# Copy to /etc/butters/butters.env, keep root:butters mode 0640, and set only
# values that are deliberately enabled. Never commit the populated file.
# Non-secret deployment values (allowed origins, repository inspection root)
# belong in /etc/butters/butters.conf, not here.
OPENAI_API_KEY=
BUTTERS_ADMIN_IDENTITIES=
