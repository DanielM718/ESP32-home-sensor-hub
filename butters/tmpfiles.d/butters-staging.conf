# The service owns runtime state; the operator group may only traverse to the
# systemd-owned validation socket. No unit independently re-chowns this path.
d /run/butters-staging 0750 butters-staging butters-staging-ops -
